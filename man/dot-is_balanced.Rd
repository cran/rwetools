% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_ps_psweights.R
\name{.is_balanced}
\alias{.is_balanced}
\title{Is a covariate balanced at the given threshold?}
\usage{
.is_balanced(std_diff, threshold)
}
\arguments{
\item{std_diff}{Numeric (scalar or vector) standardized mean difference, raw scale.}

\item{threshold}{Numeric balance threshold on the raw scale (e.g. 0.1).}
}
\value{
Logical of the same length as \code{std_diff}; \code{NA} propagates.
}
\description{
Compares the absolute standardized mean difference against the threshold on
the \emph{raw} (0-1) SMD scale produced by \code{\link{build_table1}}. This is
the single source of truth for the "Balanced" verdict used by the weighting,
matching, and fine-stratification balance tables (it replaces an earlier
\code{abs(SMD) < threshold * 100} comparison that silently passed every
covariate on raw-scale input).
}
\keyword{internal}
